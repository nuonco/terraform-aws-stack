# Uncomment and configure the backend block below to store Terraform state
# in an S3 bucket. Replace the placeholder values with your own.
#
# terraform {
#   backend "s3" {
#     bucket         = "my-terraform-state-bucket"
#     key            = "nuon/install/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "my-terraform-state-locks"
#   }
# }
